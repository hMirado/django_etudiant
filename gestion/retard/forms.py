# from django.forms import ModelForm, TextInput, Select
# from django.forms.utils import ErrorList
# from .models import Retard
#
# class RetardForm(ModelForm):
#     class Meta:
#         model = Retard
#         fields = ["niveau", "matiere", "identifiant"]
#         widget = {
#             'identifiant': Select(attrs={'class': 'border border-gray-500 rounded-lg'}),
#             'niveau': Select(attrs={'class': 'w-full border border-gray-500 rounded-lg'}),
#             'matiere': TextInput(attrs={'class': 'w-full border border-gray-500 rounded-lg'}),
#         }
